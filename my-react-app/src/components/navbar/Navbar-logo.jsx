import React from 'react';
import './Navbar-Logo.css'; // Import the CSS for the Logo component

const Logo = () => {
    return (
        <div className="logo">
            <h2>Kemi</h2>
            <h2>Kemi</h2>
        </div>
    );
};

export default Logo;
